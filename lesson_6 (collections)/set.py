# Set is like a list, can be referenced, appended and deleted like a list element.
# Uses {} as oppose to [].  Otherwise used in same was as list.
# Less resource intensive than using lists

fruit = {"Apple", "Banana", "Pear"}
for item in fruit:
    print(item)