# Tuple is like a list only it is immutable (unchangeable) after creation
# Declare Tuple with a () as oppose to a ist which is []
# Tuple uses less resources to run than a list

colours = ("Red", "Blue", "Green")
print(colours[0]) # Red
print(colours[1]) # Blue
print(colours[2]) # Green

# Can't append nor delete values